// import { fromJS } from 'immutable';
// import { makeSelectColoresDomain } from '../selectors';

// const selector = makeSelectColoresDomain();

describe('makeSelectColoresDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
