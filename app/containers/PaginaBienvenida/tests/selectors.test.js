// import { fromJS } from 'immutable';
// import { makeSelectPaginaBienvenidaDomain } from '../selectors';

// const selector = makeSelectPaginaBienvenidaDomain();

describe('makeSelectPaginaBienvenidaDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
