// import { fromJS } from 'immutable';
// import { makeSelectAlumnosDomain } from '../selectors';

// const selector = makeSelectAlumnosDomain();

describe('makeSelectAlumnosDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
