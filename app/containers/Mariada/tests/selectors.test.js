// import { fromJS } from 'immutable';
// import { makeSelectMariadaDomain } from '../selectors';

// const selector = makeSelectMariadaDomain();

describe('makeSelectMariadaDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
